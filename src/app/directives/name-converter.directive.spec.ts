import { NameConverterDirective } from './name-converter.directive';

describe('NameConverterDirective', () => {
  it('should create an instance', () => {
    const directive = new NameConverterDirective();
    expect(directive).toBeTruthy();
  });
});
